import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MinijuegoPeleasAlbertoIonic',
  webDir: 'dist'
};

export default config;
